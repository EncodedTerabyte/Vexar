const vscode = require('vscode');

function activate(context) {
    console.log('Vexar language support is now active!');
}

function deactivate() {}

module.exports = {
    activate,
    deactivate
}